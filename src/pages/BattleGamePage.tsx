import PlaceholderPage from '../components/PlaceholderPage';
import { Swords } from 'lucide-react';

export default function BattleGamePage() {
  return (
    <PlaceholderPage
      title="Jang"
      description="Real-time bellashuv"
      icon={<Swords size={48} strokeWidth={1.5} />}
    />
  );
}
