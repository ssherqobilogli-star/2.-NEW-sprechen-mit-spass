import PlaceholderPage from '../components/PlaceholderPage';
import { Swords } from 'lucide-react';

export default function BattlePage() {
  return (
    <PlaceholderPage
      title="Bellashuv"
      description="Do'stlaringiz bilan bellashing!"
      icon={<Swords size={48} strokeWidth={1.5} />}
    />
  );
}
