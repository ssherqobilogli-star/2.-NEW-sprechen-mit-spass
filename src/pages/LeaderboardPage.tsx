import PlaceholderPage from '../components/PlaceholderPage';
import { Trophy } from 'lucide-react';

export default function LeaderboardPage() {
  return (
    <PlaceholderPage
      title="Reyting"
      description="Barcha o'quvchilar reytingi"
      icon={<Trophy size={48} strokeWidth={1.5} />}
    />
  );
}
