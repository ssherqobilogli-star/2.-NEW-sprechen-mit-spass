import PlaceholderPage from '../components/PlaceholderPage';
import { Crown } from 'lucide-react';

export default function SubscriptionPage() {
  return (
    <PlaceholderPage
      title="Obuna"
      description="Pro tariflar va imkoniyatlar"
      icon={<Crown size={48} strokeWidth={1.5} />}
    />
  );
}
